<% @requests.each do |r| %>
	<% @u = User.find(r.sender_id) %>		
	<%= @u.name %>
	<%= link_to 'Accept',accept_friend_path(r)%>
	<%= link_to 'Reject',reject_friend_path(r)%><br>
<%end%>
<%= link_to 'Back', user_path(current_user) %>