<% @requests.each do |r| %>
	<% @u = User.find(r.sender_id) %>		
	<%= @u.name %>
	<%= link_to 'Accept',friend_path(r,{:status => :added}),method: :put%>
	<%= link_to 'Reject',friend_path(r,{:status => :rejected}),method: :put%><br>
<%end%>
<%= link_to 'Back', user_path(current_user) %>